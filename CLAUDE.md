# МИНГИ-ТАУ — память проекта (CLAUDE.md)

> Обновлено: 11.08.2026 (вечер), Веха 46 (подготовка к запуску:
> соглашение v2.0 синхронизировано с политикой — п.2а; механизм
> реального удаления построен и испытан — п.2б). Ранее в тот же день
> Веха 45: авария (ВМ стояла из-за неуплаты, IP сменился — теперь
> СТАТИЧЕСКИЙ 158.160.58.139), сайт обновлён до Вехи 44. ⚠️ После
> правки terms.tsx сайт снова отстаёт — обновить цепочкой А-Б-В при
> зачистке тестовых анкет (п.2в). План запуска: реальная регистрация с
> 15.08.2026, все текущие анкеты ТЕСТОВЫЕ, уведомление РКН подать ДО
> 15.08. Прежнее название — «Диаспора».
> ⚠️ 11.08 CLAUDE.md дважды коммитился в устаревшей версии (GitHub
> отдал Клоду кэш недельной давности) — файл восстановлен из локальной
> копии Мурата. Правило «всегда прикреплять CLAUDE.md в чат» — железное.

## Что это за проект

**Минги-Тау** — закрытое мобильное/веб-приложение (сообщество) для карачаевцев
и балкарцев по всему миру. Вход только по приглашению. Подпись бренда:
«Карачаево-Балкарское сообщество». Проект некоммерческий.

Основатель проекта — **Мурат Курджиев**, не программист. Правила работы с ним:

- Строго пошагово, одно действие за раз, без жаргона.
- Окружение: Windows, PowerShell, VS Code, папка проекта `C:\dev\Diaspora`.
- Отправка на GitHub: Source Control → Commit → Sync Changes.
- Всегда давать готовые файлы (не просить писать код руками).
- Одиночные файлы называть по пути с `__` вместо `/`
  (пример: `app__(tabs)__index.tsx`), всегда писать куда положить и как
  переименовать. Больше двух файлов — zip-архив со структурой папок.
- Запросы для Supabase давать короткими кусками (длинные функции редактор
  режет с ошибкой про dollar-quoted string), метки `$$` делать разными.
- Мелкая визуальная правка — сначала просить скриншот, не перебирать вслепую.
- После каждой вехи обновлять CLAUDE.md. После трёх вех в одном чате —
  напомнить открыть новый чат и подготовить стартовый текст.
- Второстепенные пожелания записывать в раздел «Отложенные задачи».
- ⚠️ Ссылки на GitHub могут отдаваться Клоду из устаревшего кэша —
  свежий CLAUDE.md и файлы кода ВСЕГДА прикреплять в чат. Даже git clone
  может отдать старьё (поймано 11.08: клон был без Вех 41–44) —
  прикреплённый файл ГЛАВНЕЕ любого клона.
- Работа с сервером: вход `ssh -i $env:USERPROFILE\.ssh\mingi-tau
murat@158.160.58.139`. Перед вставкой команд смотреть на начало строки:
  `PS C:\...` — компьютер, `murat@mingi-tau-server` — сервер.
- Панель Studio снаружи ЗАКРЫТА. Открывать через туннель (команда на
  компьютере): `ssh -i $env:USERPROFILE\.ssh\mingi-tau -L 8000:localhost:8000
murat@158.160.58.139`, затем в браузере `http://localhost:8000`
  (логин murat). Закрыл окно туннеля — панель снова недоступна.
- ⚠️ При неуплате Яндекс останавливает ВМ; после оплаты она сама НЕ
  включается: Compute Cloud → mingi-tau-server → Запустить, затем на
  сервере `cd ~/supabase && docker compose up -d`. IP теперь статический
  (закреплён 11.08.2026) — при остановках больше не уплывёт.

## Технологии

- Expo / React Native + expo-router (TypeScript), веб-версия работает.
- Supabase self-hosted НА СВОЕЙ ВМ Яндекса: авторизация, база, Storage
  (аватары), Edge Functions. Адрес для приложения:
  `https://api.mingi-tau.ru` (задаётся в `.env` проекта).
- GitHub: https://github.com/Docmurat/diaspora-app
- Пакеты сверх базовых: `react-native-svg`, `expo-blur`, `expo-font`,
  `@expo-google-fonts/philosopher`, `@expo/vector-icons`,
  `expo-image-manipulator`, `expo-clipboard`.
- `app.json`: name «Минги-Тау» (slug/scheme прежние: diaspora-app /
  diasporaapp — НЕ менять).
- **Переезд на серверы Яндекса ЗАВЕРШЁН технически (Веха 29):**
  приложение работает через свой сервер. Осталось: юридический чек-лист
  по ПДн и вывод из эксплуатации старого облачного Supabase (см. раздел
  «ПЕРЕЕЗД» и «Следующие шаги»).

## ПОДКЛЮЧЕНИЕ ПРИЛОЖЕНИЯ (Веха 29) — .env, БЕЗ ключей в репозитории

- `lib/supabase.ts` и `lib/supabase.web.ts` читают адрес и ключ из
  переменных `EXPO_PUBLIC_SUPABASE_URL` и `EXPO_PUBLIC_SUPABASE_ANON_KEY`.
  Если они не заданы — приложение падает с понятной ошибкой.
- Значения лежат в файле `.env` В КОРНЕ ПРОЕКТА (его НЕТ в репозитории,
  закрыт через `.gitignore`). Образец без секретов — `.env.example`
  (он в репозитории). Anon-ключ смотреть на ВМ:
  `grep '^ANON_KEY=' ~/supabase/.env`.
- После любой правки `.env` перезапускать так: `npx expo start -c`
  (`-c` обязателен — чистит кэш).
- В `.gitignore` добавлена строка `.env` (раньше был только `.env*.local`
  — простой `.env` УТЁК БЫ в публичный репозиторий).

## БЕЗОПАСНОСТЬ (важно)

- Репозиторий **публичный**. Никакие пароли, ключи и секреты не должны
  попадать в файлы проекта — только в `.env` проекта (нет в репозитории)
  и в `~/supabase/.env` на ВМ Яндекса.
- Снаружи на ВМ открыты ТОЛЬКО порты 22 (SSH), 80 и 443 (Caddy/https) —
  группа безопасности `mingi-tau-firewall` в Яндекс Клауде (VPC).
  Порты 5432, 6543, 8000, 8443 из интернета закрыты. Панель Studio —
  только через SSH-туннель (команда в правилах работы выше).
- 01.08.2026 из README удалён пароль почты (пароль сменён). Старый пароль
  навсегда остался в истории Git — не восстанавливать.
- 02.08.2026 сброшен пароль базы данных облачного Supabase (старый был
  утерян). Пароль хранится у Мурата вне проекта.
- 03.08.2026 данные ящика (`YANDEX_SMTP_USER`, `YANDEX_SMTP_PASS` —
  пароль приложения Яндекс-почты) вписаны в `~/supabase/.env` на ВМ;
  в чат и в репозиторий не попадали.
- Резервные копии и выгрузки данных хранить в `C:\dev\Backups` —
  ВНЕ папки проекта, чтобы данные людей не попали в публичный репозиторий.
- Перед каждым коммитом бегло проверять, что в файлах нет паролей
  и что `.env` не появился в Source Control.
- 08.08.2026 включён RLS на `moderation_messages` (Веха 43): правила
  mm_select / mm_insert; UPDATE и DELETE запрещены всем (политик нет,
  приложение их не использует). Откат одной командой:
  `alter table public.moderation_messages disable row level security;`

## ПОДТВЕРЖДЕНИЕ ПОЧТЫ (Веха 8, работает на ВМ с Вех 28–29)

Почта подтверждается **своим** 6-значным кодом (не механизмом Supabase) —
полностью на ВМ Яндекса.

- Ящик проекта: `mingi-tau-id@yandex.ru`, SMTP `smtp.yandex.ru:465`,
  пароль приложения «Почта», в ящике включён IMAP/SMTP.
- Код функции в репозитории: `supabase/functions/send-email-code/index.ts`
  (первая строка `// @ts-nocheck` — файл для Deno, красные подчёркивания
  VS Code — норма).
- На ВМ функция лежит в `~/supabase/volumes/functions/send-email-code/`.
  Секреты — строки `YANDEX_SMTP_USER` / `YANDEX_SMTP_PASS` в
  `~/supabase/.env`, проброшены в контейнер в `docker-compose.yml`
  (раздел `environment:` службы functions, после `JWT_SECRET`).
  После смены секретов: `cd ~/supabase && docker compose up -d functions`.
- ⚠️ В `~/supabase/.env` ОБЯЗАТЕЛЬНО `ENABLE_EMAIL_AUTOCONFIRM=true`:
  подтверждение у нас своё, а при `false` служба auth после регистрации
  сама шлёт письмо через несуществующий тестовый ящик `supabase-mail`
  и ВСЯ регистрация падает с пустой ошибкой `{}` (поймано и починено
  в Вехе 29). Там же `SITE_URL` и `API_EXTERNAL_URL` =
  `https://api.mingi-tau.ru`.
- Таблица `public.email_verification_codes`, RLS включён.
- Edge Function `send-email-code` (send / verify): код 10 минут, пауза
  60 сек, 5 попыток, старые коды удаляются.
- **Письмо: тема и имя отправителя ТОЛЬКО ЛАТИНИЦЕЙ** (кириллица в
  заголовке — крякозябры; ручное кодирование RFC 2047 — двойное
  кодирование, не делать). Русский текст — внутри письма.
- Поток регистрации: контакты → код → анкета. Инвайт тратится при
  финальной отправке. Телефон НЕ подтверждаем (SMS платные).

## ЗАДУМАННЫЕ ФУНКЦИИ ПРИЛОЖЕНИЯ (итоговый замысел)

1. **Каталог людей** — анкета, поиск, личные сообщения. ✅ работает
2. **Стена помощи** — лента тем, уведомления специалистам. ⏳ в планах
3. **Статьи** — традиции, фамилии, советы. ⏳ в планах
4. **Полезные ресурсы**. ⏳ в планах
5. **Афиша**. ⏳ в планах

Сквозное: инвайты, модерация, жалобы и блокировки, обращения к
администрации, избранное, правила и соглашение, уведомления, донаты.

## КАРТА НАВИГАЦИИ (утверждена, Веха 10)

**Верх (все вкладки):** слева аватар → профиль, справа колокольчик со
счётчиком → «Уведомления».

**Низ — ровно 4 вкладки** (больше не добавлять):

1. **Люди** — главный экран: поиск (список сразу не показываем), счётчик
   «Нас уже N», пирамида новых участников, «Все / Мои», кнопка
   «Пригласить», «Поддержать проект» (пока «Скоро»), подпись основателя.
   Нажатие на вкладку открывает список сразу.
2. **Помощь** — заглушка «Скоро»; метка — точка.
3. **Знания** — пилюли Статьи · Афиша · Ресурсы; заглушка «Скоро».
4. **Чаты** — личные сообщения; метка — число непрочитанных (ещё нет).

**За аватаром (кабинет):** анкета, Мои инвайты, Модерация (со счётчиком),
Настройки, Соглашение, Политика.

**Уведомления в два слоя:** метки на вкладках + колокольчик со списком,
нажатие ведёт к источнику. Своя таблица (переносимая).

## ДИЗАЙН-СИСТЕМА (утверждена, основополагающая для ВСЕХ экранов)

Светлый, воздушный, пастельный «iOS / liquid glass». Логотип-гора и
**текмет** (круг + ромб + точка).

**Живой фон с боке:** welcome, splash, pending-approval, profile-deleted
и «Люди» до начала поиска. Экраны с формами и рабочие — белые `#FFFFFF`.

**Стекло (Glass) — точечно** (поля, кнопки, отдельные карточки).
В СПИСКАХ НЕ ИСПОЛЬЗОВАТЬ (дёргается прокрутка) — карточки списков
обычные белые (радиус 18, абрис `rgba(93,140,120,0.28)` 0.75).

**Иконки:** текмет, зелёный `#69B78D`; apple-touch/maskable на белой
подложке. Файлы: `assets/images/`, `public/icons/`.

### Общие файлы

- `components/mingi.tsx` — Glass, Blob, useDrift, Tekmet, MingiBackground
  (уникальный `idPrefix`). Импорт глобально отключает браузерную рамку
  фокуса в вебе.
- `components/MingiTabBar.tsx` — своя панель вкладок (только иконки;
  активная `#3F6B5B`, остальные `#A8BDB1`; массив `TABS` внутри).
- `components/TopBar.tsx` — аватар → профиль, колокольчик → уведомления
  (`unreadCount`, `transparent`, `centerContent`). Живой счётчик — через
  `liveService` (Веха 41), самолечение общее; выбрасыванием при
  удалении/блокировке занимается часовой AccountGuard, не TopBar.
  Капкан Вехи 29 («cannot add postgres_changes callbacks after
  subscribe()» при совпадении имён каналов) с Вехи 41 закрыт в самой
  службе: liveService сам делает имя каждого канала уникальным.
- `components/AvatarCrop.tsx` — окно кадрирования: перетаскивание,
  масштаб − / +, круг, 512×512 JPEG; `prepareAvatarSource()` сразу
  уменьшает фото (≤ 1600 px). Подключён в register, edit-profile и
  moderation-edit-profile.
- `components/locations.tsx` — ЛОКАЦИИ (Веха 24), см. раздел ниже.

### Палитра

- Фон живых экранов: `#F4FAF4`; фон форм: `#FFFFFF`
- Заголовки: `#3F6B5B`; вторичный текст (капс, letterSpacing 2.5): `#719686`
- Текст на стекле / подсказки: `#4E7364`
- Кнопка primary: `rgba(105,183,141,0.92)`, текст `#FFFFFF`, тень `#69B78D`
- Кнопка secondary: белая/стекло, абрис, текст `#3F6B5B`
- Поля: `rgba(255,255,255,0.95)`, радиус 16, абрис `rgba(93,140,120,0.45)`
  0.75, плейсхолдер `#8FA79A`, текст `#2F4A3C`
- Карточки списков: белые, абрис `rgba(93,140,120,0.28)`, радиус 18
- Служебный: `#7E988B`; ссылки: `#96AC9E`; ошибки/опасное: `#C05B4D`;
  янтарный (предупреждение): `#E0A33E`
- Боке: `#A8D8C0`, `#C2E3CF`, `#DFEFE3`, `#B7DEC8`, `#CDE8D6`

### Шрифты

**Philosopher** (OFL): `Philosopher_700Bold` — заголовки,
`Philosopher_400Regular` — подписи; `useFonts` внутри экрана, до загрузки
пустой фон. Системный — кнопки и служебный текст (fontWeight 600).

### Шаблон экрана с формой

Заголовок Philosopher 34 → подпись капсом → текмет → поля →
primary-кнопка → ссылки. Радиусы: кнопки 18, поля 16. Обязательные
поля — `*` + строка «Поля со звёздочкой (\*) обязательны»; кнопки
неактивны (opacity 0.7), пока не заполнено. Alert с «Да/Нет» в браузере
НЕ работает — подтверждения делать вторым нажатием.

### Веб и PWA

`app/+html.tsx` — заголовок, theme-color, манифест, мета-теги iPhone.
`public/manifest.json`. Установка появится после публикации веб-версии
(https для API уже есть; для PWA нужен деплой самого приложения на
`mingi-tau.ru` — отдельная веха).

## ЛОКАЦИИ (Веха 24) — несколько стран и городов

Человек может жить в нескольких местах. Всё в `components/locations.tsx`:

- **Хранение** — база НЕ менялась: пары лежат в прежних полях
  `users.country` / `users.city` списками через запятую В ОДИНАКОВОМ
  ПОРЯДКЕ («Россия, Турция» + «Москва, Стамбул»). Порядок гарантирует
  интерфейс ввода; руками поля не редактируются.
- Функции: `parseLocations` (поля → пары; старые записи читаются как одна
  пара), `joinLocations` (пары → поля), `formatLocations` («Москва
  (Россия) · Стамбул (Турция)»), `firstCity` (основной город для подписи),
  `locationsValid` (нет полузаполненных пар).
- Компонент `LocationFields` — пары «Страна\* + Город\*», «＋ Живу ещё в
  одной стране / городе», у добавленных пар «Убрать».
- Подключено: register, edit-profile, moderation-edit-profile (ввод);
  user-profile и список «Людей» (показ). Поиск не трогали — ищет по
  вхождению и находит по любой стране/городу.

## ОБРАЩЕНИЯ К АДМИНИСТРАЦИИ (Вехи 23–24)

Раньше `contact-admin` писал сообщение как часть заявки на вступление —
у одобренных/удалённых карточки в очереди нет, обращение терялось.
Теперь:

- Таблица `public.appeals` (user*id, status open/closed, assigned*\_,
  closed\_\_, review_note). RLS: свои читает хозяин, всё — модераторы;
  вставка только своих; правка — модераторы. Включена в realtime.
- Сообщения — в `moderation_messages` с `request_type='appeal'`,
  `request_id` = id обращения.
- `contact-admin` маршрутизирует: анкета в очереди/на доработке — по-старому
  в карточку заявки; удалённый — обращение + текст «модератор свяжется по
  телефону» (уведомлений он не видит!); остальные — обращение + текст про
  колокольчик.
- В модерации ОТДЕЛЬНОЙ ВКЛАДКИ НЕТ (решение владельца): обращения идут
  общим конвейером — свободные в «Новом» (ярлык и фильтр «Обращения»),
  взятые в «В работе», закрытые в «Завершено» (пометка «Закрыто»,
  переписка внутри, кнопки «Изучить» нет — старый экран деталей про них
  не знает). В карточке: автор (нажатие → профиль), переписка, поле
  ответа, «Взять в работу» / «Закрыть». «Закрыть» срабатывает СРАЗУ, без
  модального окна; пояснения модератор пишет через «Ответить».
- «Одна переписка — одна карточка»: новое сообщение человека добавляется
  в его ОТКРЫТОЕ обращение (даже если оно в работе); новая карточка
  создаётся только после закрытия старой.
- **Обращения удалённых видит ТОЛЬКО основатель** (решает о
  восстановлении): фильтр в сервисе (`author.is_deleted`), в счётчике и в
  уведомлениях. У удалённого автора вместо поля ответа — пояснение и
  только «Закрыть».
- Сервис (`moderationService`): getOpenAppeals, getClosedAppeals,
  takeAppeal, closeAppeal, replyToAppeal, getAppealMessages,
  sendAppealMessage (для участника), restoreUser; обращения входят в
  счётчик задач модерации.
- Триггеры в базе: `trg_appeal_message` → `notify_on_appeal_message`
  (участник написал → модераторам, `is_moderation_task=true`, ссылка в
  new/in_progress; удалённый написал → только основателю, заголовок
  «Обращение удалённого участника»; модератор ответил → автору);
  `trg_appeal_closed` → `notify_on_appeal_closed` (закрытие → автору,
  с review_note). Старый `notify_moderators_on_user_message` пропускает
  `request_type='appeal'` (иначе был дубль уведомлений).

## RLS НА ПЕРЕПИСКАХ МОДЕРАЦИИ (Веха 43) + ЧЕК-ЛИСТ ПРОВЕРКИ

`moderation_messages` хранит три типа переписок (request_type →
request_id): `invite_request` → id участника (регистрация) ИЛИ id
строки invite_requests (просьба инвайта от анонима, экран
«Запросить инвайт»); `appeal` → id обращения; `name_change_request`
→ id запроса смены имени. Правила (по образцу appeals):

- mm_select: модераторы — всё; участник — свои переписки всех трёх
  типов, КРОМЕ строк `is_internal=true` (внутренние заметки).
- mm_insert: модераторы — всё; участник — свои сообщения
  (author_user_id = он) в свою заявку и своё обращение; аноним —
  ТОЛЬКО системный след (`author_role='system'`, author_user_id null,
  request_type='invite_request') — без этой ветки ломается приём
  просьб об инвайте.
- UPDATE/DELETE: политик нет = запрещены всем (код их не использует).
- Триггеры уведомлений — SECURITY DEFINER, RLS их не задевает.
  Рубильник «Выходной» работает: глушилка стоит на notifications.

**ЧЕК-ЛИСТ живой проверки переписок/модерации** (гонять после любых
правок RLS, триггеров уведомлений или экранов переписок):

1. Аноним (инкогнито) отправляет «Запросить инвайт» — проходит.
2. Модератору само приходит уведомление «Запрос инвайта», нажатие —
   прицел на карточку (подсветка + прокрутка).
3. На карточке ярлык «Запрос инвайта», в фильтрах «Нового» — «Инвайты».
4. Модерация: диалоги в карточках читаются.
5. Участник пишет в открытое обращение — уходит, карточка модератора
   оживает сама.
6. Ответ модератора сам появляется у участника в contact-admin.
7. Закрытие обращения — у участника экран сам возвращается к форме.
8. Экран ожидания (pending): переписка заявки видна, дописка уходит.
9. contact-admin открывается без скачка: крутилка → сразу нужный вид.

## УДАЛЕНИЕ И ВОССТАНОВЛЕНИЕ ПРОФИЛЕЙ (Веха 23)

- Удаление мягкое: `is_deleted`, `deleted_by`, `deleted_at` — данные
  остаются в базе. Удаляет и восстанавливает ТОЛЬКО основатель.
- `restoreUser` (сервис) снимает все три отметки.
- Карточка удалённого (`user-profile`): красная плашка «АККАУНТ УДАЛЁН»;
  меню «⋮» — только справка (дата регистрации, кто пригласил), без
  действий; вместо «Написать» и закладки — кнопка «Восстановить»
  (только основателю; модератор кнопок не видит).
- `profile-deleted` — живой фон, кнопки связи и выхода; через живое
  обновление + опрос раз в 7 сек сам уводит человека в приложение после
  восстановления (без перезагрузки).
- **Самоудаление — ТОЛЬКО в Настройках** (вторым нажатием); из
  edit-profile ссылка «Удалить аккаунт» с окном подтверждения вырезана
  (Веха 33). Учётка входа при мягком удалении остаётся: заново
  зарегистрироваться на ту же почту нельзя — путь один, через
  восстановление основателем (осознанно, чтобы не обходили модерацию).
- **Выход из аккаунта** — в кабинете `(tabs)/profile`, последняя строка
  меню, вторым нажатием; из Настроек выход убран (Веха 33).
- **Реальное уничтожение (Веха 46, РАБОТАЕТ):** через 30 дней после
  мягкого удаления чистильщик обезличивает запись НАВСЕГДА — см.
  раздел «ЧИСТИЛЬЩИК ПДн». Восстановление основателем возможно ТОЛЬКО
  в 30-дневном окне; после — данных физически нет.

## ЧИСТИЛЬЩИК ПДн (Веха 46) — реальное уничтожение через 30 дней

Исполняет п. 6.2 политики v2.0. Решения Мурата: сообщения в чужих
чатах ОБЕЗЛИЧИВАЮТСЯ (остаются у собеседников, автор — «Удалённый
участник»); журнал согласий ХРАНИТСЯ (после обезлички записи сами
становятся обезличенными — привязаны к безымянному скелету строки).

**Устройство (скелет строки users НЕ удаляется** — на него смотрят
чаты, жалобы, обращения, consents; удаляется только содержимое):

- `users.purged_at` — метка «вычищен»; таблица
  `public.deleted_email_hashes` — след: sha256-хэш почты (сам адрес
  не восстановим), RLS включён без политик (доступ только служебный).
- Функция `public.purge_deleted_users()` (security definer): берёт
  всех с `is_deleted`, `purged_at is null`, `role <> 'owner'`
  (основателя не тронет НИКОГДА) и `deleted_at` старше 30 дней;
  пишет хэш почты; удаляет notifications, favorites, user_blocks,
  name_change_requests; обнуляет ВСЕ личные поля анкеты (имя →
  «Удалённый участник», phone_visible → false); удаляет учётку
  `auth.users`. Переписки moderation_messages и appeals остаются
  (автор — обезличенный скелет).
- Скрипт `~/purge-deleted-users.sh`: СНАЧАЛА удаляет файлы аватаров
  через Storage API (службным ключом с localhost; путь берёт как
  `${p##*avatars/}` — в avatar_path лежит ПОЛНАЯ ссылка https://…,
  поймано испытанием), ПОТОМ зовёт функцию. Дневник — `~/purge.log`.
- Будильник: cron `20 3 * * *` (03:20, после автоснимка 03:00;
  `crontab -l` — посмотреть). Если ВМ стояла выключенной — чистка
  пройдёт в ближайшие 03:20 после включения.
- Испытано 11.08 на тестовой анкете: обезличка, хэш, учётка,
  файл аватара, сохранность consents — всё чисто. НЕ проверено (нет
  живых чатов): вид «Удалённый участник» в диалоге собеседника —
  проверить в вехе чата.

## Статус экранов

В дизайн-системе: welcome (Glass ещё локальный старый; подпись
основателя внизу), splash, login,
invite, request-invite, register (3 шага, кроп, умная сфера, ЛОКАЦИИ,
ТРИ пункта ПДн на шаге 3 — Веха 44),
pending-approval, contact-admin (маршрутизация обращений, переписка
открытого обращения, без скачка), consent и memorandum (Веха 44),
edit-profile (ЛОКАЦИИ, без самоудаления), change-email, change-password,
settings (только удаление вторым нажатием; выход переехал в кабинет),
notifications (liveService, крестик удаления, группы по датам — Веха 42),
(tabs)/\_layout,
(tabs)/index («Люди»: живой фон до поиска, липкий поиск, пирамида,
счётчик, показ локаций парами, подпись основателя), (tabs)/help и
knowledge (заглушки),
(tabs)/chats, (tabs)/profile (кабинет со счётчиком модерации и выходом),
user-profile (режим модерации, блокировки, удалённые, локации),
invites, privacy (политика v2.0 — Веха 44), terms (v2.0 от
15.08.2026, синхронизировано с политикой — Веха 46), moderation (фильтры «Нового» +
«Обращения» + «Инвайты», цветные полосы, рубильник «Выходной», живое
обновление), moderation-edit-profile (дизайн-система + кроп, Веха 24),
report-user, profile-deleted (автовыход после восстановления),
request-name-change (Веха 34), access-restricted (Веха 34: свои
обращения вместо Телеграма, автовозврат после снятия блокировки).

Ещё в старом стиле: `chat`, `moderation-case-details`.
`test-db.tsx` — старый тестовый экран, удалить (в отложенных).

Прочее: `sessionService` — `.maybeSingle()`; `moderationService` —
approve/reject И block/unblock проверяют, что база приняла изменение.

## УВЕДОМЛЕНИЯ (Веха 13+) — своя таблица, переносимая

- `public.notifications`: user_id, type, title, body, link, is_read,
  created_at, `is_moderation_task` (признак задачи модерации — по нему
  работает рубильник). RLS: свои — хозяин; вставка — любой вошедший
  (ужесточить позже).
- `notificationService`: getMyNotifications, getUnreadCount, markRead,
  markAllRead, delete, createNotification (не роняет основное действие).
- Все уведомления модерации создаются ТРИГГЕРАМИ В БАЗЕ. Ссылки ведут
  прямо к карточке: `/moderation?tab=<очередь>&focus=<id>`.
- Модераторам: новая заявка, анкета исправлена, жалоба, смена имени,
  сообщение участника (без дубля за 30 сек), обращение (только не от
  удалённых; от удалённых — только основателю), запрос инвайта
  (триггер `trg_invite_request_created` на `invite_requests`,
  SECURITY DEFINER — работает и от анонима; Веха 43).
- Участнику: решения по анкете/имени/жалобе, инвайт использован (с
  Вехи 40 — только ПОСЛЕ одобрения анкеты), личное сообщение, ответ
  администрации, обращение закрыто (информационное, без ссылки).
- Приватность: заявителю не сообщают меры; нарушителю не сообщают ничего.
- **Рубильник «Выходной»**: `users.moderation_notifications_enabled` +
  `users.moderation_muted_at`; глушилка `skip_muted_moderation_notice`
  кладёт модерационные уведомления тихо (is_read=true), триггер
  `wake_moderation_notices` при выключении будит накопленное (Веха 39).
  Кнопка в шапке модерации.
- Экран уведомлений (Веха 42): liveService (тихая перезагрузка
  списка), крестик удаления одним нажатием (optimistic + возврат при
  ошибке), группы СЕГОДНЯ · ВЧЕРА · 7 ДНЕЙ · 30 ДНЕЙ · РАНЕЕ.

## ИНВАЙТЫ (Веха 15)

Разделы: Созданные · Переданные · Пришли. Любая передача кода
(копирование, Телеграм, WhatsApp — прямые ссылки, БЕЗ системного меню
«Поделиться», в браузере оно валится) помечает `sent_at`. «Пришли» —
после одобрения анкеты (Веха 40: уведомление пригласившему и список —
по одобрению, не по регистрации). Удаление — вторым нажатием.

## ЖИВОЕ ОБНОВЛЕНИЕ (Веха 16)

`services/liveService.ts` — единственное место с Supabase Realtime
(с Вехи 41 на нём же колокольчик TopBar и рубеж 2 часового
AccountGuard; с Вехи 42 — экран уведомлений; с Вехи 43 —
contact-admin; своя подписка осталась только у экрана `chat` — уйдёт в
веху чата). Экраны знают только
`subscribeToChanges(имя, [таблицы], onChange)`.

**Самолечение (Веха 41):** при статусах CHANNEL_ERROR / TIMED_OUT /
CLOSED служба сама пересоздаёт канал с паузой (3 с, при повторных
сбоях удваивается до потолка 30 с, после успеха сбрасывается); после
восстановления вызывает `onChange` — экран перечитывает данные и
догоняет пропущенное за время обрыва. Уникальные имена каналов служба
делает сама (сквозной номер) — экранам выдумывать их больше не нужно.
⚠️ Капкан, пойманный живым тестом: выброшенный канал перед смертью
кричит «CLOSED»; служба слушает ТОЛЬКО действующий канал (`channel !==
myChannel` → игнор), иначе — бесконечный хоровод замен. Проверка
самолечения: `cd ~/supabase && docker compose restart realtime` на ВМ
и смотреть доклады «Живой эфир: …» в консоли браузера. Обкатано в бою
11.08 (авария: каналы сами поднялись после старта ВМ).

В трансляции: `notifications`, `users`, `moderation_messages`,
`invite_requests`, `name_change_requests`, `complaints`, `appeals`.

На ВМ Яндекса publication `supabase_realtime` со всеми 7 таблицами
работает — проверено сквозной проверкой в Вехе 29 и серверной
диагностикой в Вехе 35 (publication, replica identity, RLS,
realtime.subscription — всё чисто).

Живут на живом обновлении: колокольчик и уведомления, ожидание
одобрения, модерация, contact-admin, profile-deleted,
access-restricted (автовозврат), **AccountGuard** (см. ниже). Кнопок
«Обновить» нет — всё обновляется само, тихо.

**Глобальный часовой `components/AccountGuard.tsx` (Веха 35):**
невидимый компонент в корне (`app/_layout.tsx`), слушает СВОЮ запись
`users` и при `is_deleted`/`is_blocked` уводит на
profile-deleted/access-restricted С ЛЮБОГО экрана. Тройная защита
(Веха 40, НЕ упрощать): 1) осмотр записи сразу при входе; 2) живые
обновления — с Вехи 41 через liveService (самолечение общее); 3) опрос-страховка раз в 12 сек. Понимает вход/выход
(onAuthStateChange), не пересоздаёт канал при обновлении токена, не
дёргается, если человек уже на нужном экране. Раньше этим занимался
TopBar — но он стоит не везде (на главной не выбрасывало); теперь
TopBar слушает только `notifications` (колокольчик).

⚠️ Таблица `users` целиком в трансляции — при чувствительных полях сузить.

## ПРАВИЛА ДОСТУПА И БЛОКИРОВОК (Веха 17, дополнено Вехой 35)

**Две разные блокировки:**

1. **Личная** (участник ↔ участник, `userBlockService`): скрывают
   контакты друг друга, писать нельзя. Заблокировавший (А) видит на
   карточке Б серую строчку «Участник заблокирован» — напоминание себе.
   Б и остальные НЕ видят ничего; единственный намёк для Б — в чате
   пропадает поле ввода, БЕЗ пояснений (решение владельца).
2. **Административная** (`is_blocked`, кнопка у админов): человек
   отключён от сообщества — часовой уводит его на `access-restricted`
   (оттуда — обращение к администрации и автовозврат после снятия).
   Правило в базе прячет его из списков и карточек для обычных
   участников. Красный чип «ЗАБЛОКИРОВАН» под аватаром видят ТОЛЬКО
   основатель и модераторы. В меню «⋮» пункт-переключатель:
   «Заблокировать участника» / «Снять блокировку участника»
   (список заблокированных со снятием есть и в Модерации).
   Обращения от заблокированных в модерации: пометка и только
   «Закрыть», без поля ответа (решение владельца, Веха 35).

- Модератор видит скрытые контакты, только пока его не заблокировали.
- **Основателю доступно всё и всегда** — блокировки на него не действуют.
- Удаление/восстановление — только основатель (см. раздел выше). Вход
  удалённого/заблокированного перехватывают `app/index.tsx` и
  `login.tsx`; на живую это делает часовой `AccountGuard` (Веха 35).

## Вехи

1–5. ✅ Восстановление, Git, ребрендинг, дизайн-система, формы входа. 6. ✅ Имя «Минги-Тау», иконки, подготовка PWA. (01.08) 7. ✅ Сквозная проверка: инвайт → регистрация → модерация → каталог. (01.08) 8. ✅ Подтверждение почты своим кодом. (01.08) 9. ✅ Вкладки на дизайн-системе. (01.08) 10. ✅ Карта навигации + безопасность (пароль из README). (01.08) 11. ✅ Каркас 4 вкладок и главный экран. (01.08) 13. ✅ Уведомления: таблица, служба, экран, счётчик. (01.08) 15. ✅ Инвайты: экраны и логика передачи. (01.08) 16. ✅ Живое обновление через liveService. (01.08) 17. ✅ Модерация: вид и правила; правила блокировок. (01.08) 18. ✅ Уведомления до конца: триггеры, ссылки на заявку, «Выходной». (01.08) 19. ✅ Карточка человека, инвайты, документы в дизайн-системе. (01.08) 21. ✅ Избранное внутри «Людей». (01.08) 22. ✅ Профиль-кабинет и настройки. (01.08) 23. ✅ **Обращения к администрации + восстановление профилей:** таблица
appeals, конвейер через «Новое → В работе → Завершено», ответы и
закрытие без модалки, обращения удалённых только основателю,
плашка и кнопка «Восстановить» в карточке, живой автовыход с
profile-deleted, фикс дубля уведомлений. (02.08) 24. ✅ **Локации парами + правка анкеты модератором:**
`components/locations.tsx`, несколько стран/городов во всех формах и
показ пар в карточке и списке; `moderation-edit-profile` в
дизайн-системе с нашим кропом. (02.08) 25. ✅ **Переезд, шаг 1 — страховочная копия базы:** все 14 таблиц
выгружены в CSV в `C:\dev\Backups` (вне репозитория); пароль базы
сброшен и сохранён у Мурата; установлен pg_dump (PostgreSQL 17
Command Line Tools). Полная копия с домашнего ПК не прошла —
см. подробности в разделе переезда. (02.08) 26. ✅ **Переезд: ВМ создана, долг шага 1 закрыт:** каталог `mingi-tau`
и ВМ `mingi-tau-server` в Яндекс Клауде (Ubuntu 24.04, 2 vCPU /
4 ГБ / SSD 40 ГБ, ~3 499 ₽/мес, IP тогда 84.201.133.133 — с
11.08.2026 статический 158.160.58.139, вход по
SSH-ключу `mingi-tau`, логин `murat`); с ВМ снята ПОЛНАЯ копия базы
(pg_dump 17, прошла с первой попытки) и скачаны все аватары из
Storage (14 файлов); копии в `C:\dev\Backups` и на ВМ. (03.08) 27. ✅ **Переезд: свой Supabase запущен.** На ВМ установлен Docker,
развёрнут self-hosted Supabase (папка `~/supabase`), скриптом
`~/make-secrets.sh` сгенерированы и вписаны в `.env` все секреты
(пароль базы ВМ, JWT, anon/service_role, пароль панели — записаны
у Мурата). Все 11 служб healthy. (03.08) 28. ✅ **Переезд, шаг 3 — база, аватары и почтальон на своей ВМ:**
полная копия залита в self-hosted Supabase; проверено: все 14 таблиц
`public` с данными (82 анкеты, 31 учётка `auth.users`), 42 правила
RLS, 17 триггеров, 7 таблиц в трансляции Realtime; 14 аватаров
загружены скриптом `~/upload-avatars.sh` с сохранением путей
`avatars/<id>/<файл>.jpg`, публичная ссылка проверена; Edge Function
`send-email-code` положена в репозиторий
(`supabase/functions/send-email-code/index.ts`) и на ВМ, секреты SMTP
в `~/supabase/.env` + проброс в docker-compose.yml; боевое письмо
с кодом отправлено и получено. (03.08) 29. ✅ **Переезд, шаг 4 — домен, https, щит, переключение приложения:**
домен `mingi-tau.ru` (Руцентр) делегирован на DNS Яндекс Клауда,
A-запись `api` → ВМ; Caddy раздаёт `https://api.mingi-tau.ru` с
автосертификатом и ведёт на порт 8000; группа безопасности
`mingi-tau-firewall` закрыла снаружи всё, кроме 22/80/443; автоснимки
диска `daily-snapshot` (каждый день 03:00, хранить 7); приложение
переведено на `.env` (URL + anon key, `.gitignore` дополнен строкой
`.env`); в `~/supabase/.env` включён `ENABLE_EMAIL_AUTOCONFIRM=true`
(иначе регистрация падала с `{}`), `SITE_URL`/`API_EXTERNAL_URL` — на
https-адрес; фикс realtime-канала TopBar (уникальные имена); сквозная
проверка пройдена: вход, регистрация с кодом, модерация, аватары,
живое обновление. (03.08)

30. ✅ **Деплой веб-версии:** A-запись `mingi-tau.ru` → ВМ (Cloud DNS;
    с 11.08.2026 — 158.160.58.139); сборка
    `npx expo export --platform web` → папка `dist`;
    файлы на ВМ в `/var/www/mingi-tau` (владелец caddy); в Caddyfile
    добавлен сайт `mingi-tau.ru` (root + try_files /index.html +
    file_server, gzip); https-сертификат получен автоматически; проверено
    с компьютера и телефона, PWA ставится на главный экран.
    **Обновление сайта после правок кода:** 1) на компьютере
    `npx expo export --platform web`; 2) `scp -i $env:USERPROFILE\.ssh\mingi-tau -r C:\dev\Diaspora\dist murat@158.160.58.139:~/site-new`; 3) на сервере `sudo rm -rf /var/www/mingi-tau/* && sudo cp -r ~/site-new/dist/* /var/www/mingi-tau/ 2>/dev/null || sudo cp -r ~/site-new/* /var/www/mingi-tau/; sudo chown -R caddy:caddy /var/www/mingi-tau`
    (команда шага В учитывает, что при повторной отправке scp кладёт
    файлы во вложенную папку `site-new/dist`).
    (03.08)

31. ✅ **Редизайн главной и списка (`app/(tabs)/index.tsx`,
    `components/mingi.tsx`).** Главная: убраны «Новые участники» и TopBar;
    кнопка «Поддержать проект» — компактное розовое стекло с отступом от
    статус-бара; счётчик «Нас уже N» под строкой поиска; подпись основателя
    внутри страницы (без подложки), опущена к низу; поиск компактнее
    (высота 44). Список: строка поиска зафиксирована сверху (заголовок
    «Поиск» убран), поле во всю ширину с крестиком очистки (кнопка
    «Сбросить поиск» упразднена — на главную теперь можно попасть только
    при запуске приложения); вкладки «Все / Избранные» в две колонки с
    плавно скользящей линией 80×2 (Animated; блок хуков стоит ДО ранних
    выходов — иначе падение!); переключение вкладок очищает запрос;
    закладки мгновенные (optimistic-обновление + откат при ошибке сервера,
    hitSlop 14); кнопка добавления человека 44×44; текмет в футере списка;
    карточки подняты вплотную к вкладкам. Фон (`mingi.tsx`): пятна вдвое
    меньше (380/420/400); на вебе слой пятен `position: fixed` с
    ПИКСЕЛЬНОЙ высотой, замеренной при загрузке — размеры меняются только
    при повороте экрана, клавиатура фон не двигает; в meta viewport
    добавлен `interactive-widget=resizes-visual`; метка веб-стилей
    `mingi-web-css-v7`. Сайт после коммита НЕ обновлялся — обновить одним
    заходом после следующей порции правок. (04.08)

32. ✅ **Шлифовка списка, профиль человека, фон добит (`app/(tabs)/index.tsx`,
    `components/mingi.tsx`, `app/user-profile.tsx`).** Список: карточки
    упразднены — открытые строки с тонкими разделителями
    (rgba(93,140,120,0.18)), воздух paddingVertical 16, имя обычным
    системным шрифтом (16.5/600; Philosopher остался в декоративных
    заголовках); под линией вкладок 10px воздуха. Фон: слой пятен создаётся
    высотой во ВЕСЬ экран устройства (max(innerHeight, screen.height)) —
    исчезла белая граница внизу, когда браузер прячет адресную строку при
    прокрутке; пересчёт только при повороте. Профиль человека
    (user-profile): «← Назад» и меню подняты к верху (insets.top + 10
    вместо paddingTop 56, хук useSafeAreaInsets ДО ранних выходов), аватар
    96×96, имя Philosopher 21, кнопка «Написать» и закладка 42 высотой,
    шрифты кнопок 14.5 (вкл. модераторские). Сайт обновлён цепочкой А-Б-В.
    (04.08)

33. ✅ **Выход и удаление наведены (`(tabs)/profile`, `settings`,
    `edit-profile`).** «Выйти из аккаунта» — в кабинете, последней
    строкой меню, красным, вторым нажатием; из Настроек выход убран.
    Самоудаление — только в Настройках: из edit-profile вырезаны ссылка
    «Удалить аккаунт», окно подтверждения, состояния и стили. (05.08)

34. ✅ **Дизайн-система: `request-name-change` и `access-restricted`.**
    Смена имени: карточка «СЕЙЧАС В ПРОФИЛЕ», стеклянные поля, защита от
    двойной отправки, экран успеха с текметом. «Доступ ограничен»: живой
    фон, кнопка «Написать администрации» ВМЕСТО Телеграма (меньше
    зависимостей; заодно починена заглушка tg-адреса), автовозврат после
    снятия блокировки (живое обновление + опрос раз в 7 сек). (05.08)

35. ✅ **Блокировки доведены до ума; глобальный часовой
    (`AccountGuard`, `_layout`, `TopBar`, `user-profile`, `moderation`,
    `moderationService`).** Найдено охотой с диагностикой сервера
    (publication, replica identity, RLS, realtime.subscription — чисто):
    живое выбрасывание работало только на экранах с TopBar — на главной
    не выбрасывало. Решение: невидимый `components/AccountGuard.tsx` в
    корне слушает свою запись `users` на любом экране; TopBar разгружен
    (только колокольчик). user-profile: пункт-переключатель
    «Заблокировать/Снять блокировку участника», красный чип
    «ЗАБЛОКИРОВАН» (видят только админы). Модерация: карточки
    сворачиваются ТОЛЬКО шапкой (исчез глюк «нажал на поле — карточка
    свернулась»), обращения от заблокированных — пометка и только
    «Закрыть», в архиве добавлены переходы на профили (участник, автор
    обращения, обе стороны жалобы — случайно закрытая задача больше не
    тупик). Сервис: block/unblock проверяют, что база приняла изменение;
    в выборки обращений добавлен `is_blocked`. (05.08)

36. ✅ **Парящая панель вкладок + главная (`components/MingiTabBar.tsx`,
    4 вкладки, `index.tsx`).** Панель — капсула в стиле Telegram:
    position absolute ПОВЕРХ экрана, плотный белый, зелёный абрис, тень;
    под ней SVG-дымка (белый снизу → прозрачный сверху, id градиента
    `mingiTabBarFade`), контент проезжает под капсулой и тает; у
    прокручиваемых вкладок paddingBottom 120 (`index`, `chats`,
    `profile`, `favorites`) — в кабинете важно докручивать «Выйти».
    Главная: кнопка «Помочь проекту» (бывш. «Поддержать проект») +
    мелкая подпись «Идеи, замечания, поддержка проекта» (текст
    согласован перебором вариантов); у строки поиска и кнопки-стрелки
    стекло убрано (обычное белое поле / зелёная кнопка, стили
    `inputPlain`, `searchButtonPlain`, `glassInputProps` удалён);
    Glass остался у «Помочь проекту» и FAB приглашения. Сайт утром
    обновлён до Вехи 35 цепочкой А-Б-В. (05.08)

37. ✅ **Изгнан «выброс на главную» (`access-restricted`,
    `profile-deleted`).** Симптом: с любого экрана вне вкладок
    (профиль человека, настройки) выкидывало на первый экран через
    ~7 сек, тихо, без ошибок. Виновник: перепроверка «блокировку
    сняли/профиль восстановили?» (опрос раз в 7 сек, Веха 34),
    пережившая свой экран. Лечение на обоих экранах: перед переброской
    проверка pathname (перебрасывать только СО СВОЕГО экрана,
    `usePathname` + ref), после срабатывания таймер глушит себя сам.
    Найдено живым тестом Мурата (наблюдение «через 7 секунд» решило
    дело). (05.08)

38. ✅ **Уведомления админа: ревизия, прицел, следование
    (`liveService`, `TopBar`, `moderation.tsx`, 2 триггера в базе).**
    Полная опись 17 триггеров: админам приходят новая заявка /
    повторная подача / ответ на доработку / жалоба / смена имени /
    сообщение в чате модерации (обращения и апелляции — своими
    триггерами, без дублей); пользователям — решения по жалобе и
    имени, «инвайт использован», плюс из кода — одобрение/отклонение
    и личные сообщения. liveService и колокольчик теперь докладывают
    статус подписки в консоль («Живой эфир: …» / «Колокольчик: …») —
    раньше отказ сервера глотался молча; в журнале realtime
    периодические subscription_errors и «Killing transport pids» —
    подписки иногда падают, лечение (самолечение) в «Следующих
    шагах». Триггеры починены (через psql heredoc):
    `notify_moderators_on_complaint` — ссылка
    `focus=complaint-<id жалобы>` (было: id обвиняемого — мимо ключа
    карточки), `notify_moderators_on_name_change` —
    `tab=new&focus=name_change-<id>` (было голое `/moderation`).
    Модерация: прицельная карточка из уведомления подсвечивается
    (стиль `cardFocused`, гаснет за 6 сек) и список прокручивается к
    ней (`onLayout` + `scrollRef`); «Взять в работу» (все 5 видов) —
    экран сам переходит на «В работе», карточка раскрыта и подсвечена
    (`followToInProgress`); «На доработку» НАРОЧНО без прыжка —
    решение Мурата: туда не смотрят, ждут ответа человека. Обкатка
    чек-листа админ-сценариев начата: №1 жалоба и №7 апелляция
    пройдены. (05.08)

39. ✅ **Обкатка уведомлений продолжена; «Выходной» отложить-и-разбудить
    (`notifications.tsx`, `moderation.tsx`, `moderationService`,
    `(tabs)/profile.tsx`, триггеры в базе).** Пройдены №8 (решение по
    жалобе — жалобщику, информационное, без перехода — так задумано),
    №2+№9 (смена имени в обе стороны: прицел и следование работают),
    №13 «Выходной». Починки: экран уведомлений падал «cannot add
    postgres_changes callbacks after subscribe()» — имя канала теперь
    уникальное на каждый заход (капкан Вехи 29); в отклонении смены
    имени терялся комментарий модератора — функция
    `notify_user_on_name_change_decision` вклеивает `review_note`
    («Имя осталось прежним. Комментарий модератора: «…»»); в карточку
    «Смена ФИО» добавлена кликабельная строка участника
    (`requester:user_id` в выборках сервиса и refreshOneNameChange,
    `renderPersonRow` как у жалобы); шапки модерации, уведомлений и
    кабинета подняты к верху (insets.top + 10, образец Вехи 32; хук ДО
    ранних выходов!); из модерации убран заголовок «Модерация» с
    текметом, у канбана paddingTop 14. **«Выходной» был шредером** —
    глушилка возвращала null, уведомления за выходной пропадали
    насовсем. Теперь: `users.moderation_muted_at` (момент ухода);
    `skip_muted_moderation_notice` в выходной кладёт модерационные
    уведомления тихо (is_read=true); новый триггер
    `wake_moderation_notices` на users при выключении будит всё
    накопленное (is_read=false с момента muted_at). Граница сохранена:
    касается ТОЛЬКО is_moderation_task (ссылки /moderation%) — чаты,
    личные и будущие комментарии под выходной не попадают. Нюанс:
    пробуждённые сортируются по времени создания. Функции за
    триггерами ищутся через pg_trigger (имена не совпадают с именами
    триггеров!). (05.08)
40. ✅ **Дообкатка админ-сценариев: закрыты №3, №4, №5, №6, №10, №11,
    №12 (из чек-листа остался только №14).** Найдено и починено:
    **№3 обращения** — участник теперь видит переписку открытого
    обращения прямо в `contact-admin` (пузырьки «Вы»/«Администрация»,
    живое обновление, при закрытии экран сам возвращается к форме;
    телефон при дописке не дублируется); уведомление «Обращение
    закрыто» стало информационным — в триггере
    `notify_on_appeal_closed` ссылка заменена на null (правка прямо в
    базе). **Инвайты** — уведомление пригласившему и попадание в
    список «Пришли» перенесены с момента регистрации на момент
    ОДОБРЕНИЯ: в базе триггер `notify_inviter_on_invite_used` (на
    invites) заменён на `notify_inviter_on_user_approved` (на users,
    pending→approved), а `getMyInvitedUsers` фильтрует по
    `moderation_status='approved'`. **№10 повторная подача после
    доработки** — сопроводительное сообщение необязательно; след в
    «Диалоге» остаётся всегда (текст участника или автотекст «Анкета
    отправлена повторно после исправлений»); признак «уже отправлено»
    на экране ожидания — серверный флажок `moderator_has_unread_changes`
    (гаснет, когда модератор открывает карточку); при отправке на
    доработку модератор СБРАСЫВАЕТ этот флажок (иначе новый круг
    показывался как «уже отправлено»); немые `Alert` на вебе заменены
    на плашки/красный текст прямо на экране (Alert.alert в
    react-native-web НЕ РАБОТАЕТ — молча глотает и успехи, и ошибки;
    проверять все экраны!). **№4/№5 отклонение** — экран отклонённого
    получил «Исправить анкету», поле обоснования и «Подать заявку
    повторно»: статус → pending, карточка возвращается в «Новое»
    неназначенной, модераторам сам срабатывает штатный триггер «Новая
    заявка»; кнопка «вернуть из Завершено» модератору не нужна.
    **Часовой AccountGuard — тройная защита:** осмотр записи сразу при
    входе (раньше после F5 удалённый ОСТАВАЛСЯ внутри — стартовая
    маршрутизация обходится на вебе), живые обновления и опрос-страховка
    раз в 12 сек (живое событие про удалённого до самого удалённого
    может не доходить из-за правил чтения). **Стоп-кран экрана
    ожидания:** после одобрения автопроверка (7 сек) продолжала гонять
    человека по кругу «заставка—главный экран» — теперь после первого
    перехода все проверки глохнут (`redirectedRef`). **Анкета заявки**
    («Открыть профиль») видна только модератору, взявшему заявку в
    работу, — в «Новом» кнопки нет. Капкан Вехи 29 добит везде:
    `pending-approval` и канал чата `messages:chat:` получили
    уникальные имена. Регистрацию тестовых удобно гонять через
    Gmail-псевдонимы (`почта+test1@gmail.com`) и отдельный профиль
    Chrome (некоторые браузеры не пускают к api.mingi-tau.ru). (08.08)
41. ✅ **Самолечение живых подписок (`liveService`, `TopBar`,
    `AccountGuard`) — часть 1 п.4 «Следующих шагов».** liveService:
    при CHANNEL_ERROR / TIMED_OUT / CLOSED сам пересоздаёт канал с
    растущей паузой (3 → 30 с, сброс после успеха), после
    восстановления вызывает onChange («догоняем пропущенное»);
    уникальные имена каналов теперь делает сама служба (капкан Вехи 29
    закрыт навсегда). Колокольчик TopBar и рубеж 2 часового
    AccountGuard переведены на liveService (рубежи 1 и 3 часового не
    тронуты — тройная защита Вехи 40 цела). Пойман и обезврежен капкан:
    выброшенный при замене канал перед смертью кричит «CLOSED» — без
    проверки «свой ли канал» служба пугалась собственного выброса и
    меняла каналы по кругу бесконечно (найдено живым тестом с
    `docker compose restart realtime`). Проверено: обрыв → рост паузы →
    «восстановлен, догоняем пропущенное» → тишина; счётчик колокольчика
    после лечения живой. (08.08)
42. ✅ **Колокольчик добит (`app/notifications.tsx`) — сценарий №14,
    чек-лист админ-сценариев закрыт полностью.** Экран уведомлений
    переведён на liveService (самолечение; ушёл последний ручной обход
    капкана Вехи 29 с Date.now() в имени канала): вместо вклейки
    payload — тихая перезагрузка списка load(true), без крутилки и без
    сброса списка при случайной ошибке сети. Удаление уведомления —
    серый крестик на строке, ОДНИМ нажатием (решение владельца:
    подтверждение вторым нажатием было сделано и убрано — уведомление
    не та ценность); мгновенное, optimistic + возврат строки при
    ошибке сервера (образец закладок Вехи 31); правило базы «delete
    own notifications» уже было, функция deleteNotification в службе
    уже была — менялся только экран. Разбивка списка по датам:
    СЕГОДНЯ · ВЧЕРА · ПОСЛЕДНИЕ 7 ДНЕЙ · ПОСЛЕДНИЕ 30 ДНЕЙ · РАНЕЕ
    (границы от начала суток, пустые группы не показываются, заголовки
    капсом #719686); под «Вчера» строки показывают время, а не слово
    «Вчера» второй раз. «Отметить все прочитанными» работает; ссылка
    видна ТОЛЬКО при наличии непрочитанных — так и было, это не
    пропажа (проверено с Муратом). (08.08)
43. ✅ **RLS на переписках + запрос инвайта доведён + скачок
    contact-admin (`moderation.tsx`, `contact-admin.tsx`, правила и
    триггер в базе).** Включён RLS на `moderation_messages`
    (см. раздел выше; проверка снаружи: curl с anon-ключом отдаёт `[]`
    вместо всех переписок). Найдено в обкатке: уведомления модераторам
    о просьбе инвайта НЕ БЫЛО ВОВСЕ (в описи Вехи 38 отсутствовало) —
    создан триггер `trg_invite_request_created` (SECURITY DEFINER, от
    анонима работает; прицельная ссылка focus=invite_request-<id>);
    ярлык карточки «Заявка» → «Запрос инвайта» (в обеих очередях),
    фильтр «Заявки» → «Инвайты». Скачок contact-admin (форма с
    телефоном мелькала до загрузки переписки): причина — до загрузки
    анкеты человек считался «обычным без переписки»; теперь до
    готовности анкеты И ответа про обращение — только крутилка
    (profileReady + threadReady). Подписка contact-admin переведена
    на liveService — своя подписка осталась только у чата. Чек-лист
    проверки переписок (9 пунктов) записан в раздел RLS. (08.08)
44. ✅ **Внедрение ПДн + Меморандум (`register`, `consent`,
    `memorandum`, `privacy`, `welcome`, `(tabs)/index`, `_layout`,
    `consentService`, `store/consentFlow`, таблица `consents`).**
    Подробности — в разделе «ПДн (152-ФЗ)». Решение Мурата: все
    текущие анкеты тестовые, реальная регистрация с 15.08.2026 —
    уведомление РКН подаётся ДО начала обработки, как задумано
    законом. Меморандум — идеологический документ сообщества
    (взаимопомощь, уважение, вне политики и религии, доверие, общее
    дело), написан и утверждён 08.08; принятие — только после
    открытия полного текста. Проверено: журнал пишет три строки с
    версиями; цикл «открыть → принять → снять галочку» работает.
    (08.08)
45. ✅ **Авария 11.08 закрыта: статический IP; сайт обновлён до
    Вехи 44.** За неуплату Яндекс остановил ВМ; после оплаты она сама
    не включилась (запущена вручную: Compute Cloud → Запустить, затем
    `docker compose up -d` — все 11 служб healthy). Публичный IP был
    динамическим и при перезапуске сменился на 158.160.58.139 — домен
    смотрел в пустоту, SSH кричал про чужой отпечаток ключа (по
    старому адресу 84.201.133.133 теперь ЧУЖАЯ машина, не
    подключаться). Починка: IP закреплён СТАТИЧЕСКИМ (~150–200 ₽/мес),
    обе A-записи в Cloud DNS переведены на новый адрес. Данные целы.
    Заодно: самолечение Вехи 41 обкатано в бою (каналы сами поднялись,
    доклады «Живой эфир: … подключён» в консоли), сайт обновлён
    цепочкой А-Б-В — впервые с Вехой 44 на борту (закрыт п. «г»
    подготовки к запуску; сам шаг 3 регистрации с тремя пунктами на
    сайте ещё не прогонялся — прогнать при зачистке тестовых анкет).
    ⚠️ Урок чата: GitHub отдал Клоду клон недельной давности — два
    коммита CLAUDE.md откатили файл к Вехе 40+; восстановлен из
    локальной копии Мурата этим файлом. Прикреплённый CLAUDE.md
    ГЛАВНЕЕ любого клона. (11.08)

46. ✅ **Подготовка к запуску, п.2а + п.2б: соглашение v2.0 и
    чистильщик ПДн (`app/terms.tsx`; в базе — таблица
    `deleted_email_hashes`, колонка `users.purged_at`, функция
    `purge_deleted_users()`; на ВМ — скрипт `~/purge-deleted-users.sh`
    и cron 03:20).** Соглашение переписано с нуля в 9 разделов
    зеркально политике (версия 2.0, вступление 15.08.2026 — совпадает
    с версией в журнале согласий, consentService не менялся): оператор
    и некоммерческий характер, ценз 16 лет, «один человек — одна
    учётка», принципы Меморандума («вне политики и религии»), запрет
    выносить данные участников наружу, ПДн через отдельное согласие,
    удаление с уничтожением до 30 дней, обжалование через обращения.
    Чистильщик — см. раздел «ЧИСТИЛЬЩИК ПДн»; испытан на тестовой
    анкете, попутно поймано: в avatar_path лежит полная ссылка, а не
    короткий путь — скрипт научен обоим форматам. Решения Мурата:
    чаты обезличивать, журнал согласий хранить. (11.08)

## Отложенные задачи (сделать позже)

1. Объявления на главной — если вернутся, сразу из базы.
2. «Поддержать проект»: экран «О проекте» + перевод (ссылка/QR, без
   встроенных платежей).
3. Счётчик «Нас уже N» — показывать с порога, пока сообщество маленькое.
4. Афиша — отдельное место при росте.
5. Модерация: сделать очевидным переезд карточки между вкладками.
6. Показывать участнику историю переписки его обращения в `contact-admin`
   (сейчас ответы приходят только уведомлениями).
7. Смена почты в edit-profile — через наш почтальон с кодом.
8. Welcome: локальный Glass → импорт из mingi.tsx (косметика).
9. Пуш-уведомления — после выкладки веб-версии.
10. Шрифт Philosopher в собранной PWA офлайн — проверить.
11. Убрать из журнала auth предупреждение про `GOTRUE_MAILER_EXTERNAL_HOSTS`
    (безвредное, но шумит) — добавить хост в настройку.
12. RLS на вставку в `notifications` ужесточить (сейчас — любой вошедший).
13. Уведомления об инвайте: приглашающему НЕ должно приходить «инвайт
    использован, вы пригласили человека», пока анкету не одобрила
    модерация. Слать после одобрения (скорее всего, править триггер
    в базе).
14. Удалить `app/test-db.tsx` — старый тестовый экран (и строку из
    `_layout.tsx`).
15. Шапка «Избранного» (`(tabs)/favorites.tsx`): поднять к верху как
    остальные (paddingTop 64 → insets.top + 10) — заодно с адаптивной
    вёрсткой.
16. Закладка «В избранное» в профиле человека (`user-profile.tsx`):
    срабатывает с задержкой — ждёт ответа базы. Сделать мгновенной
    по образцу списка (Веха 31: optimistic-обновление + откат при
    ошибке).
17. Экран chat: свою realtime-подписку перевести на `liveService`
    (войдёт в веху чата) — последняя своя подписка в проекте.

## ПЕРЕЕЗД НА ЯНДЕКС КЛАУД — технически ЗАВЕРШЁН (Веха 29, 03.08.2026)

**Зачем:** 152-ФЗ требует хранить персональные данные граждан РФ в базах
на территории России; Supabase-как-сервис держал базу за рубежом.
В анкетах у нас ФИО, телефон, почта, дата рождения, город, фото.

**Итог:** свой self-hosted Supabase на ВМ Яндекса, приложение работает
через `https://api.mingi-tau.ru`. Старый облачный Supabase приложением
БОЛЬШЕ НЕ ИСПОЛЬЗУЕТСЯ, но проект там ещё не выключен (см. «Следующие
шаги» — вывести из эксплуатации после страховочного периода).

**АВАРИЯ 11.08.2026 (закрыта, Веха 45):** неуплата → ВМ остановлена →
после оплаты запущена вручную → динамический IP сменился. Теперь IP
СТАТИЧЕСКИЙ: `158.160.58.139`. Профилактика — автоплатёж (в «Следующих
шагах»). Старый адрес 84.201.133.133 — больше не наш.

**Хозяйство на ВМ (шпаргалка):**

- ВМ `mingi-tau-server` (каталог `mingi-tau`): Ubuntu 24.04, 2 vCPU /
  4 ГБ / SSD 40 ГБ, ~3 499 ₽/мес, IP `158.160.58.139` — СТАТИЧЕСКИЙ
  (закреплён 11.08.2026, ~150–200 ₽/мес сверху), вход по ключу
  `mingi-tau`, логин `murat`. Скачивание файлов:
  `scp -i <ключ> murat@IP:файл C:\...`
- Supabase: папка `~/supabase` (docker compose). Управление:
  `cd ~/supabase && docker compose ps` (ps / up -d / down / restart).
  Журналы: `docker compose logs <служба> --since 30m --no-color`.
  Службы: db, auth, rest, realtime, storage, imgproxy, kong, studio,
  meta, functions, supavisor.
- Секреты — `~/supabase/.env` (пароли записаны у Мурата). Anon-ключ:
  `grep '^ANON_KEY=' ~/supabase/.env`. Важные выключатели auth:
  `ENABLE_EMAIL_AUTOCONFIRM=true`, `SITE_URL` и `API_EXTERNAL_URL` =
  `https://api.mingi-tau.ru`. После правок `.env`:
  `docker compose up -d`.
- Служебные запросы к базе — от `supabase_admin` (у `postgres` в
  self-hosted прав меньше):
  `docker exec -i supabase-db psql -U supabase_admin -d postgres -c "..."`.
  Длинные функции целиком заходят через heredoc в консоли сервера:
  `docker exec -i supabase-db psql -U supabase_admin -d postgres << 'SQL'`
  …код… `SQL` (ограничение «короткими кусками» касалось редактора Studio).
- **Домен:** `mingi-tau.ru` куплен в Руцентре (до 28.07.2027), DNS
  делегирован на `ns1.yandexcloud.net` / `ns2.yandexcloud.net`; зона
  `mingi-tau-ru` в сервисе Cloud DNS Яндекса; записи там же (A-записи
  `api` и голого домена → 158.160.58.139, переведены 11.08.2026).
- **HTTPS:** Caddy (`/etc/caddy/Caddyfile`): `api.mingi-tau.ru` →
  `reverse_proxy localhost:8000`; сертификат получает и продлевает сам.
  Перезапуск после правок: `sudo systemctl reload caddy`.
- **Щит:** группа безопасности `mingi-tau-firewall` (VPC) на интерфейсе
  ВМ: входящие только 22, 80, 443; исходящие все. Порты 5432/6543/
  8000/8443 снаружи закрыты. Группу `default-sg-...` не использовать
  (разрешает всё).
- **Панель Studio:** только через SSH-туннель (команда в правилах
  работы). Прямой адрес `http://158.160.58.139:8000` не работает
  (и не должен).
- **Автоснимки:** расписание `daily-snapshot` в Compute Cloud — каждый
  день 03:00, хранить последние 7; привязано к диску ВМ.
- **Бэкапы вручную:** `mingi-tau-full-2026-08-03.sql` и
  `avatars-backup.tar.gz` — в `C:\dev\Backups` и на ВМ (`/home/murat`,
  скрипты `~/backup-avatars.sh`, `~/upload-avatars.sh`).
- **DNS на компьютере Мурата:** Wi-Fi «Беспроводная сеть» переведён на
  справочники 8.8.8.8 и 77.88.8.8 (кэш роутера держал пустой ответ по
  новому домену). Можно оставить навсегда. Возврат при желании:
  `Set-DnsClientServerAddress -InterfaceAlias "Беспроводная сеть"
-ResetServerAddresses`.

## ПДн (152-ФЗ) — пакет v1.1 утверждён (08.08.2026)

Документы в `C:\dev\Backups` (в публичный репозиторий НЕ класть):
«Минги-Тау — ПДн, памятка для юриста.docx» и
«Минги-Тау*пакет*ПДн_v1_1.docx» (мастер-версия с правками Мурата).
Мурат — юрист по образованию, решения принимает сам.

**Принятые решения (v1.1):** оператор — физлицо (Курджиев М. А.-С.);
почта оператора murat.kurdzhiev@yandex.ru; показ анкет участникам =
ПРЕДОСТАВЛЕНИЕ определённому кругу (не распространение, ст. 10.1 не
применяется); удаление: реальное уничтожение до 30 дней + хэш почты
как след от обхода модерации; СКЗИ не применяются; возрастной ценз
16 лет (вопрос согласия законных представителей за 16–17-летних — на
контроле Мурата).

**Перед подачей уведомления РКН вписать:** ИНН, адрес регистрации,
телефон, дату начала обработки, адрес ЦОД Яндекс Клауда.

**ЗАПУСК: все текущие анкеты — ТЕСТОВЫЕ (реальных субъектов ПДн нет).
Реальная регистрация — с 15.08.2026. Перед запуском: зачистить
тестовые анкеты из базы; сайт обновлён 11.08 (Веха 45) — при
следующих правках кода обновить снова; подать уведомление РКН (дата
начала обработки в форме — 15.08.2026, подать ДО неё). Лист переноса
для формы РКН — в C:\dev\Backups.**

**Внедрение (Веха 44 — СДЕЛАНО):** шаг 3 регистрации — три пункта:

1. согласие на ПДн (отдельная галочка, ст. 9 в ред. 156-ФЗ; полный
   текст — экран consent, версия 1.0); 2) принятие соглашения +
   ознакомление с политикой; 3) Меморандум сообщества (экран memorandum,
   версия 1.0) — галочку руками поставить НЕЛЬЗЯ: строка открывает
   меморандум, зажигает её только кнопка «Принимаю» в конце текста
   (мостик store/consentFlow.ts + useFocusEffect в register); клик по
   зажжённой строке снимает галочку. Кнопка «Отправить» блокирована без
   всех трёх. Журнал согласий: таблица public.consents (append-only,
   RLS: пишет владелец про себя, читают владелец и модераторы; типы
   pdn/terms/memorandum, версии в services/consentService.ts — при
   правке текстов ПОДНИМАТЬ версию). Политика v2.0 (9 разделов,
   вступление 15.08.2026) — в privacy.tsx. Подпись «Основатель — Мурат
   Курджиев»: welcome (низ), главная, конец меморандума.

**План внедрения ВЫПОЛНЕН (Веха 46):** механизм реального удаления
работает (см. «ЧИСТИЛЬЩИК ПДн»), terms.tsx v2.0 синхронизирован с
политикой. Из подготовки к запуску остались пп. «в» и «д».

## Следующие шаги

//

1. **Вывод старого облачного Supabase из эксплуатации** (по 152-ФЗ данные
   не должны оставаться за рубежом): выждать страховочный период
   (~14–17 августа), затем свежий бэкап на всякий случай — и удалить
   данные/проект в старом Supabase. До этого НИЧЕГО в старом проекте
   не менять.
2. **ПОДГОТОВКА К ЗАПУСКУ 15.08.2026** (порядок важен):
   а) ✅ terms.tsx v2.0 синхронизирован с политикой (Веха 46);
   б) ✅ механизм реального удаления работает (Веха 46, раздел
   «ЧИСТИЛЬЩИК ПДн»);
   в) зачистить тестовые анкеты из базы (снять бэкап до зачистки!),
   обновить сайт цепочкой А-Б-В (после Вехи 46 он снова отстаёт —
   там старый terms) и прогнать регистрацию с тремя пунктами на
   боевом сайте;
   д) Мурат: вписать реквизиты в лист переноса и подать уведомление
   РКН до 15.08 (дата начала обработки — 15.08.2026).
3. **Автоплатёж в Яндекс Клауде** (или напоминание о пополнении),
   чтобы авария 11.08 не повторилась: при нуле баланса ВМ
   останавливается и всё приложение лежит.
4. **Адаптивная вёрстка:** аккуратный вид на узких экранах телефонов и
   на планшетах (сейчас дизайн смотрится крупно). Делать прицельно,
   экран за экраном, по скриншотам.
5. Чат (`app/chat.tsx`): доработать функционально, перевести на
   дизайн-систему (в кнопках ещё старая зелень #2E7D32) и на живое
   обновление через `liveService` — последняя своя подписка в проекте
   (самолечение Вехи 41 на неё пока не действует). Проверка
   блокировки уже добавлена (Веха 17). Сюда же: проверить вид
   диалога с вычищенным участником («Удалённый участник», Веха 46).
6. Метки на вкладках: число непрочитанных на «Чатах»
   (`chat_participants.last_read_at`).
7. Остатки модерации: `moderation-case-details` («Изучить») — целиком
   на СТАРОМ дизайне, перевести на дизайн-систему и научить
   обращениям; `test-db.tsx` удалить.
8. Стена помощи — первая большая новая функция.
