# МИНГИ-ТАУ — память проекта (CLAUDE.md)

> Обновлено: 03.08.2026, Веха 27 (self-hosted Supabase запущен на ВМ).
> Прежнее название — «Диаспора».

## Что это за проект

**Минги-Тау** — закрытое мобильное/веб-приложение (сообщество) для карачаевцев
и балкарцев по всему миру. Вход только по приглашению. Подпись бренда:
«Карачаево-Балкарское сообщество». Проект некоммерческий.

Основатель проекта — **Мурат Курджиев**, не программист. Правила работы с ним:

- Строго пошагово, одно действие за раз, без жаргона.
- Окружение: Windows, PowerShell, VS Code, папка проекта `C:\dev\Diaspora`.
- Отправка на GitHub: Source Control → Commit → Sync Changes.
- Всегда давать готовые файлы (не просить писать код руками).
- Одиночные файлы называть по пути с `__` вместо `/`
  (пример: `app__(tabs)__index.tsx`), всегда писать куда положить и как
  переименовать. Больше двух файлов — zip-архив со структурой папок.
- Запросы для Supabase давать короткими кусками (длинные функции редактор
  режет с ошибкой про dollar-quoted string), метки `$$` делать разными.
- Мелкая визуальная правка — сначала просить скриншот, не перебирать вслепую.
- После каждой вехи обновлять CLAUDE.md. После трёх вех в одном чате —
  напомнить открыть новый чат и подготовить стартовый текст.
- Второстепенные пожелания записывать в раздел «Отложенные задачи».
- ⚠️ Ссылки на GitHub могут отдаваться Клоду из устаревшего кэша —
  свежий CLAUDE.md и файлы кода ВСЕГДА прикреплять в чат.

## Технологии

- Expo / React Native + expo-router (TypeScript), веб-версия работает.
- Supabase: авторизация, база, Storage (аватары), Edge Functions.
- GitHub: https://github.com/Docmurat/diaspora-app
- Пакеты сверх базовых: `react-native-svg`, `expo-blur`, `expo-font`,
  `@expo-google-fonts/philosopher`, `@expo/vector-icons`,
  `expo-image-manipulator`, `expo-clipboard`.
- `app.json`: name «Минги-Тау» (slug/scheme прежние: diaspora-app /
  diasporaapp — НЕ менять).
- **Идёт переезд на серверы Яндекса** (см. раздел «ПЕРЕЕЗД НА ЯНДЕКС
  КЛАУД»). Всё новое делать так, чтобы не привязывалось к Supabase
  (см. почту, уведомления, liveService).

## БЕЗОПАСНОСТЬ (важно)

- Репозиторий **публичный**. Никакие пароли, ключи и секреты не должны
  попадать в файлы проекта — только в секреты Supabase (Edge Functions →
  Secrets) и в `.env`, которого нет в репозитории.
- 01.08.2026 из README удалён пароль почты (пароль сменён). Старый пароль
  навсегда остался в истории Git — не восстанавливать.
- 02.08.2026 сброшен пароль базы данных Supabase (старый был утерян).
  Новый пароль хранится у Мурата вне проекта. Понадобится при переносе
  базы на ВМ Яндекса.
- Резервные копии и выгрузки данных хранить в `C:\dev\Backups` —
  ВНЕ папки проекта, чтобы данные людей не попали в публичный репозиторий.
- Перед каждым коммитом бегло проверять, что в файлах нет паролей.

## ПОДТВЕРЖДЕНИЕ ПОЧТЫ (Веха 8) — собственная система, переносимая

Почта подтверждается **своим** 6-значным кодом (не механизмом Supabase) —
при переезде на Яндекс переедет целиком.

- Ящик проекта: `mingi-tau-id@yandex.ru`, SMTP `smtp.yandex.ru:465`,
  пароль приложения «Почта», в ящике включён IMAP/SMTP.
- Секреты Supabase: `YANDEX_SMTP_USER`, `YANDEX_SMTP_PASS`. После смены
  секретов функцию деплоить заново.
- Таблица `public.email_verification_codes`, RLS включён.
- Edge Function `send-email-code` (send / verify): код 10 минут, пауза
  60 сек, 5 попыток, старые коды удаляются.
- **Письмо: тема и имя отправителя ТОЛЬКО ЛАТИНИЦЕЙ** (кириллица в
  заголовке — крякозябры; ручное кодирование RFC 2047 — двойное
  кодирование, не делать). Русский текст — внутри письма.
- Поток регистрации: контакты → код → анкета. Инвайт тратится при
  финальной отправке. Телефон НЕ подтверждаем (SMS платные).

## ЗАДУМАННЫЕ ФУНКЦИИ ПРИЛОЖЕНИЯ (итоговый замысел)

1. **Каталог людей** — анкета, поиск, личные сообщения. ✅ работает
2. **Стена помощи** — лента тем, уведомления специалистам. ⏳ в планах
3. **Статьи** — традиции, фамилии, советы. ⏳ в планах
4. **Полезные ресурсы**. ⏳ в планах
5. **Афиша**. ⏳ в планах

Сквозное: инвайты, модерация, жалобы и блокировки, обращения к
администрации, избранное, правила и соглашение, уведомления, донаты.

## КАРТА НАВИГАЦИИ (утверждена, Веха 10)

**Верх (все вкладки):** слева аватар → профиль, справа колокольчик со
счётчиком → «Уведомления».

**Низ — ровно 4 вкладки** (больше не добавлять):

1. **Люди** — главный экран: поиск (список сразу не показываем), счётчик
   «Нас уже N», пирамида новых участников, «Все / Мои», кнопка
   «Пригласить», «Поддержать проект» (пока «Скоро»), подпись основателя.
   Нажатие на вкладку открывает список сразу.
2. **Помощь** — заглушка «Скоро»; метка — точка.
3. **Знания** — пилюли Статьи · Афиша · Ресурсы; заглушка «Скоро».
4. **Чаты** — личные сообщения; метка — число непрочитанных (ещё нет).

**За аватаром (кабинет):** анкета, Мои инвайты, Модерация (со счётчиком),
Настройки, Соглашение, Политика.

**Уведомления в два слоя:** метки на вкладках + колокольчик со списком,
нажатие ведёт к источнику. Своя таблица (переносимая).

## ДИЗАЙН-СИСТЕМА (утверждена, основополагающая для ВСЕХ экранов)

Светлый, воздушный, пастельный «iOS / liquid glass». Логотип-гора и
**текмет** (круг + ромб + точка).

**Живой фон с боке:** welcome, splash, pending-approval, profile-deleted
и «Люди» до начала поиска. Экраны с формами и рабочие — белые `#FFFFFF`.

**Стекло (Glass) — точечно** (поля, кнопки, отдельные карточки).
В СПИСКАХ НЕ ИСПОЛЬЗОВАТЬ (дёргается прокрутка) — карточки списков
обычные белые (радиус 18, абрис `rgba(93,140,120,0.28)` 0.75).

**Иконки:** текмет, зелёный `#69B78D`; apple-touch/maskable на белой
подложке. Файлы: `assets/images/`, `public/icons/`.

### Общие файлы

- `components/mingi.tsx` — Glass, Blob, useDrift, Tekmet, MingiBackground
  (уникальный `idPrefix`). Импорт глобально отключает браузерную рамку
  фокуса в вебе.
- `components/MingiTabBar.tsx` — своя панель вкладок (только иконки;
  активная `#3F6B5B`, остальные `#A8BDB1`; массив `TABS` внутри).
- `components/TopBar.tsx` — аватар → профиль, колокольчик → уведомления
  (`unreadCount`, `transparent`, `centerContent`); через живое обновление
  ловит удаление/блокировку и уводит человека на нужный экран.
- `components/AvatarCrop.tsx` — окно кадрирования: перетаскивание,
  масштаб − / +, круг, 512×512 JPEG; `prepareAvatarSource()` сразу
  уменьшает фото (≤ 1600 px). Подключён в register, edit-profile и
  moderation-edit-profile.
- `components/locations.tsx` — ЛОКАЦИИ (Веха 24), см. раздел ниже.

### Палитра

- Фон живых экранов: `#F4FAF4`; фон форм: `#FFFFFF`
- Заголовки: `#3F6B5B`; вторичный текст (капс, letterSpacing 2.5): `#719686`
- Текст на стекле / подсказки: `#4E7364`
- Кнопка primary: `rgba(105,183,141,0.92)`, текст `#FFFFFF`, тень `#69B78D`
- Кнопка secondary: белая/стекло, абрис, текст `#3F6B5B`
- Поля: `rgba(255,255,255,0.95)`, радиус 16, абрис `rgba(93,140,120,0.45)`
  0.75, плейсхолдер `#8FA79A`, текст `#2F4A3C`
- Карточки списков: белые, абрис `rgba(93,140,120,0.28)`, радиус 18
- Служебный: `#7E988B`; ссылки: `#96AC9E`; ошибки/опасное: `#C05B4D`;
  янтарный (предупреждение): `#E0A33E`
- Боке: `#A8D8C0`, `#C2E3CF`, `#DFEFE3`, `#B7DEC8`, `#CDE8D6`

### Шрифты

**Philosopher** (OFL): `Philosopher_700Bold` — заголовки,
`Philosopher_400Regular` — подписи; `useFonts` внутри экрана, до загрузки
пустой фон. Системный — кнопки и служебный текст (fontWeight 600).

### Шаблон экрана с формой

Заголовок Philosopher 34 → подпись капсом → текмет → поля →
primary-кнопка → ссылки. Радиусы: кнопки 18, поля 16. Обязательные
поля — `*` + строка «Поля со звёздочкой (\*) обязательны»; кнопки
неактивны (opacity 0.7), пока не заполнено. Alert с «Да/Нет» в браузере
НЕ работает — подтверждения делать вторым нажатием.

### Веб и PWA

`app/+html.tsx` — заголовок, theme-color, манифест, мета-теги iPhone.
`public/manifest.json`. Установка появится после публикации на https.

## ЛОКАЦИИ (Веха 24) — несколько стран и городов

Человек может жить в нескольких местах. Всё в `components/locations.tsx`:

- **Хранение** — база НЕ менялась: пары лежат в прежних полях
  `users.country` / `users.city` списками через запятую В ОДИНАКОВОМ
  ПОРЯДКЕ («Россия, Турция» + «Москва, Стамбул»). Порядок гарантирует
  интерфейс ввода; руками поля не редактируются.
- Функции: `parseLocations` (поля → пары; старые записи читаются как одна
  пара), `joinLocations` (пары → поля), `formatLocations` («Москва
  (Россия) · Стамбул (Турция)»), `firstCity` (основной город для подписи),
  `locationsValid` (нет полузаполненных пар).
- Компонент `LocationFields` — пары «Страна\* + Город\*», «＋ Живу ещё в
  одной стране / городе», у добавленных пар «Убрать».
- Подключено: register, edit-profile, moderation-edit-profile (ввод);
  user-profile и список «Людей» (показ). Поиск не трогали — ищет по
  вхождению и находит по любой стране/городу.

## ОБРАЩЕНИЯ К АДМИНИСТРАЦИИ (Вехи 23–24)

Раньше `contact-admin` писал сообщение как часть заявки на вступление —
у одобренных/удалённых карточки в очереди нет, обращение терялось.
Теперь:

- Таблица `public.appeals` (user*id, status open/closed, assigned*_,
  closed\__, review_note). RLS: свои читает хозяин, всё — модераторы;
  вставка только своих; правка — модераторы. Включена в realtime.
- Сообщения — в `moderation_messages` с `request_type='appeal'`,
  `request_id` = id обращения.
- `contact-admin` маршрутизирует: анкета в очереди/на доработке — по-старому
  в карточку заявки; удалённый — обращение + текст «модератор свяжется по
  телефону» (уведомлений он не видит!); остальные — обращение + текст про
  колокольчик.
- В модерации ОТДЕЛЬНОЙ ВКЛАДКИ НЕТ (решение владельца): обращения идут
  общим конвейером — свободные в «Новом» (ярлык и фильтр «Обращения»),
  взятые в «В работе», закрытые в «Завершено» (пометка «Закрыто»,
  переписка внутри, кнопки «Изучить» нет — старый экран деталей про них
  не знает). В карточке: автор (нажатие → профиль), переписка, поле
  ответа, «Взять в работу» / «Закрыть». «Закрыть» срабатывает СРАЗУ, без
  модального окна; пояснения модератор пишет через «Ответить».
- «Одна переписка — одна карточка»: новое сообщение человека добавляется
  в его ОТКРЫТОЕ обращение (даже если оно в работе); новая карточка
  создаётся только после закрытия старой.
- **Обращения удалённых видит ТОЛЬКО основатель** (решает о
  восстановлении): фильтр в сервисе (`author.is_deleted`), в счётчике и в
  уведомлениях. У удалённого автора вместо поля ответа — пояснение и
  только «Закрыть».
- Сервис (`moderationService`): getOpenAppeals, getClosedAppeals,
  takeAppeal, closeAppeal, replyToAppeal, getAppealMessages,
  sendAppealMessage (для участника), restoreUser; обращения входят в
  счётчик задач модерации.
- Триггеры в базе: `trg_appeal_message` → `notify_on_appeal_message`
  (участник написал → модераторам, `is_moderation_task=true`, ссылка в
  new/in_progress; удалённый написал → только основателю, заголовок
  «Обращение удалённого участника»; модератор ответил → автору);
  `trg_appeal_closed` → `notify_on_appeal_closed` (закрытие → автору,
  с review_note). Старый `notify_moderators_on_user_message` пропускает
  `request_type='appeal'` (иначе был дубль уведомлений).

## УДАЛЕНИЕ И ВОССТАНОВЛЕНИЕ ПРОФИЛЕЙ (Веха 23)

- Удаление мягкое: `is_deleted`, `deleted_by`, `deleted_at` — данные
  остаются в базе. Удаляет и восстанавливает ТОЛЬКО основатель.
- `restoreUser` (сервис) снимает все три отметки.
- Карточка удалённого (`user-profile`): красная плашка «АККАУНТ УДАЛЁН»;
  меню «⋮» — только справка (дата регистрации, кто пригласил), без
  действий; вместо «Написать» и закладки — кнопка «Восстановить»
  (только основателю; модератор кнопок не видит).
- `profile-deleted` — живой фон, кнопки связи и выхода; через живое
  обновление + опрос раз в 7 сек сам уводит человека в приложение после
  восстановления (без перезагрузки).

## Статус экранов

В дизайн-системе: welcome (Glass ещё локальный старый), splash, login,
invite, request-invite, register (3 шага, кроп, умная сфера, ЛОКАЦИИ),
pending-approval, contact-admin (маршрутизация обращений),
edit-profile (ЛОКАЦИИ), change-email, change-password, settings
(удаление — вторым нажатием), notifications, (tabs)/\_layout,
(tabs)/index («Люди»: живой фон до поиска, липкий поиск, пирамида,
счётчик, показ локаций парами), (tabs)/help и knowledge (заглушки),
(tabs)/chats, (tabs)/profile (кабинет со счётчиком модерации),
user-profile (режим модерации, блокировки, удалённые, локации),
invites, privacy, terms, moderation (фильтры «Нового» + «Обращения»,
цветные полосы, рубильник «Выходной», живое обновление),
moderation-edit-profile (дизайн-система + кроп, Веха 24), report-user,
profile-deleted (автовыход после восстановления).

Ещё в старом стиле: `chat`, `moderation-case-details` и прочие
`moderation-*`, `request-name-change`, `access-restricted`.

Прочее: `sessionService` — `.maybeSingle()`; `moderationService` —
approve/reject проверяют, что база приняла изменение.

## УВЕДОМЛЕНИЯ (Веха 13+) — своя таблица, переносимая

- `public.notifications`: user_id, type, title, body, link, is_read,
  created_at, `is_moderation_task` (признак задачи модерации — по нему
  работает рубильник). RLS: свои — хозяин; вставка — любой вошедший
  (ужесточить при переезде).
- `notificationService`: getMyNotifications, getUnreadCount, markRead,
  markAllRead, delete, createNotification (не роняет основное действие).
- Все уведомления модерации создаются ТРИГГЕРАМИ В БАЗЕ. Ссылки ведут
  прямо к карточке: `/moderation?tab=<очередь>&focus=<id>`.
- Модераторам: новая заявка, анкета исправлена, жалоба, смена имени,
  сообщение участника (без дубля за 30 сек), обращение (только не от
  удалённых; от удалённых — только основателю).
- Участнику: решения по анкете/имени/жалобе, инвайт использован, личное
  сообщение, ответ администрации, обращение закрыто.
- Приватность: заявителю не сообщают меры; нарушителю не сообщают ничего.
- **Рубильник «Выходной»**: `users.moderation_notifications_enabled`;
  триггер `skip_muted_moderation` BEFORE INSERT глушит записи с
  `is_moderation_task=true`. Кнопка в шапке модерации.

## ИНВАЙТЫ (Веха 15)

Разделы: Созданные · Переданные · Пришли. Любая передача кода
(копирование, Телеграм, WhatsApp — прямые ссылки, БЕЗ системного меню
«Поделиться», в браузере оно валится) помечает `sent_at`. «Пришли» —
после одобрения анкеты. Удаление — вторым нажатием.

## ЖИВОЕ ОБНОВЛЕНИЕ (Веха 16) — ВАЖНО ПРИ ПЕРЕЕЗДЕ

`services/liveService.ts` — единственное место с Supabase Realtime.
Экраны знают только `subscribeToChanges(имя, [таблицы], onChange)`.
При переезде переписывается только этот файл (внутри можно вернуть опрос
по таймеру).

В трансляции: `notifications`, `users`, `moderation_messages`,
`invite_requests`, `name_change_requests`, `complaints`, `appeals`.

Живут на живом обновлении: колокольчик и уведомления, ожидание
одобрения, модерация, profile-deleted, TopBar (ловит удаление/блокировку).
Кнопок «Обновить» нет — всё обновляется само, тихо.

⚠️ Таблица `users` целиком в трансляции — при чувствительных полях сузить.

## ПРАВИЛА ДОСТУПА И БЛОКИРОВОК (Веха 17)

- Модератор видит скрытые контакты, только пока его не заблокировали.
- **Основателю доступно всё и всегда** — блокировки на него не действуют.
- В чате при блокировке исчезает поле ввода, БЕЗ пояснений (решение
  владельца).
- Удаление/восстановление — только основатель (см. раздел выше). Вход
  удалённого перехватывают `app/index.tsx` и `login.tsx` →
  `profile-deleted`.

## Вехи

1–5. ✅ Восстановление, Git, ребрендинг, дизайн-система, формы входа. 6. ✅ Имя «Минги-Тау», иконки, подготовка PWA. (01.08) 7. ✅ Сквозная проверка: инвайт → регистрация → модерация → каталог. (01.08) 8. ✅ Подтверждение почты своим кодом. (01.08) 9. ✅ Вкладки на дизайн-системе. (01.08) 10. ✅ Карта навигации + безопасность (пароль из README). (01.08) 11. ✅ Каркас 4 вкладок и главный экран. (01.08) 13. ✅ Уведомления: таблица, служба, экран, счётчик. (01.08) 15. ✅ Инвайты: экраны и логика передачи. (01.08) 16. ✅ Живое обновление через liveService. (01.08) 17. ✅ Модерация: вид и правила; правила блокировок. (01.08) 18. ✅ Уведомления до конца: триггеры, ссылки на заявку, «Выходной». (01.08) 19. ✅ Карточка человека, инвайты, документы в дизайн-системе. (01.08) 21. ✅ Избранное внутри «Людей». (01.08) 22. ✅ Профиль-кабинет и настройки. (01.08) 23. ✅ **Обращения к администрации + восстановление профилей:** таблица
appeals, конвейер через «Новое → В работе → Завершено», ответы и
закрытие без модалки, обращения удалённых только основателю,
плашка и кнопка «Восстановить» в карточке, живой автовыход с
profile-deleted, фикс дубля уведомлений. (02.08) 24. ✅ **Локации парами + правка анкеты модератором:**
`components/locations.tsx`, несколько стран/городов во всех формах и
показ пар в карточке и списке; `moderation-edit-profile` в
дизайн-системе с нашим кропом. (02.08) 25. ✅ **Переезд, шаг 1 — страховочная копия базы:** все 14 таблиц
выгружены в CSV в `C:\dev\Backups` (вне репозитория); пароль базы
сброшен и сохранён у Мурата; установлен pg_dump (PostgreSQL 17
Command Line Tools). Полная копия с домашнего ПК не прошла —
см. подробности в разделе переезда. (02.08) 26. ✅ **Переезд: ВМ создана, долг шага 1 закрыт:** каталог `mingi-tau`
и ВМ `mingi-tau-server` в Яндекс Клауде (Ubuntu 24.04, 2 vCPU /
4 ГБ / SSD 40 ГБ, ~3 499 ₽/мес, IP 84.201.133.133, вход по
SSH-ключу `mingi-tau`, логин `murat`); с ВМ снята ПОЛНАЯ копия базы
(pg_dump 17, прошла с первой попытки) и скачаны все аватары из
Storage (14 файлов); копии в `C:\dev\Backups` и на ВМ. (03.08) 27. ✅ **Переезд: свой Supabase запущен.** На ВМ установлен Docker,
развёрнут self-hosted Supabase (папка `~/supabase`), скриптом
`~/make-secrets.sh` сгенерированы и вписаны в `.env` все секреты
(пароль базы ВМ, JWT, anon/service_role, пароль панели — записаны
у Мурата). Все 11 служб healthy; панель Studio открывается на
`http://84.201.133.133:8000` (логин murat). (03.08)

## Отложенные задачи (сделать позже)

1. Объявления на главной — если вернутся, сразу из базы.
2. «Поддержать проект»: экран «О проекте» + перевод (ссылка/QR, без
   встроенных платежей).
3. Счётчик «Нас уже N» — показывать с порога, пока сообщество маленькое.
4. Афиша — отдельное место при росте.
5. Модерация: сделать очевидным переезд карточки между вкладками.
6. Показывать участнику историю переписки его обращения в `contact-admin`
   (сейчас ответы приходят только уведомлениями).
7. Смена почты в edit-profile — через наш почтальон с кодом.
8. Welcome: локальный Glass → импорт из mingi.tsx (косметика).
9. Пуш-уведомления — после выкладки на https.
10. Шрифт Philosopher в собранной PWA офлайн — проверить.

## ПЕРЕЕЗД НА ЯНДЕКС КЛАУД — решение зафиксировано 02.08.2026

**Зачем:** 152-ФЗ требует хранить персональные данные граждан РФ в базах
на территории России; Supabase-как-сервис держит базу за рубежом.
В анкетах у нас ФИО, телефон, почта, дата рождения, город, фото.

**Выбран вариант А:** арендуем виртуальную машину в Яндекс Клауде
(дата-центры в РФ; у Мурата там уже два проекта) и поднимаем на ней
СВОЙ Supabase (self-hosted, открытый и бесплатный код). Код приложения
почти не меняется — меняется адрес подключения. Вариант Б (пересборка
на родные сервисы Яндекса) отклонён: недели работы, переписывание
авторизации, дороже (один Managed PostgreSQL — от ~5,5 тыс. ₽/мес).

**Бюджет:** ВМ ~2 vCPU / 4 ГБ / 40–50 ГБ SSD ≈ 2 500–3 500 ₽/мес
(точно покажет калькулятор при создании ВМ); снимки диска — копейки;
домен ~200–600 ₽/год; самому Supabase не платим ничего. Первые 100 ГБ
трафика в месяц бесплатно. Ответственность: обновления, бэкапы и
«упало—подними» становятся нашими — обязательны автоснимки по
расписанию и инструкция на случай сбоя.

**Порядок серии вех:**

1. ✅ РЕЗЕРВНАЯ КОПИЯ базы Supabase — ЗАКРЫТО ПОЛНОСТЬЮ (Вехи 25–26):
   - CSV всех 14 таблиц (SQL Editor) → `C:\dev\Backups` (02.08).
   - ПОЛНАЯ копия pg_dump (схема, данные, RLS, триггеры, функции):
     `mingi-tau-full-2026-08-03.sql` — снята с ВМ Яндекса, лежит в
     `C:\dev\Backups` и на ВМ в `/home/murat` (03.08).
   - Аватары Storage: 14 файлов, скрипт `~/backup-avatars.sh` на ВМ
     (через service_role ключ), архив `avatars-backup.tar.gz` в
     `C:\dev\Backups` и папка на ВМ (03.08).
   - Памятка (если понадобится копия с домашнего ПК): pooler Supabase
     обрывает pg_dump с домашних сетей — снимать копии С ВМ, там
     проходит сразу. Порт 6543 — только короткие запросы, 5432 — dump.
2. ✅ Создание ВМ и установка self-hosted Supabase — ЗАКРЫТО:
   - ✅ Каталог `mingi-tau`, ВМ `mingi-tau-server`: Ubuntu 24.04,
     Intel 2 vCPU / 4 ГБ RAM / SSD 40 ГБ, ~3 499 ₽/мес,
     зона ru-central1-a, публичный IP `84.201.133.133`.
   - Вход: `ssh -i $env:USERPROFILE\.ssh\mingi-tau murat@84.201.133.133`
     (ключ `mingi-tau` в `C:\Users\<user>\.ssh\`, отдельный от других
     проектов). Скачивание файлов с ВМ: `scp -i <ключ> murat@IP:файл C:\...`
   - На ВМ установлены: postgresql-client-17 (+ официальный источник
     apt.postgresql.org), jq, Docker (docker без sudo для murat).
   - ✅ Self-hosted Supabase развёрнут: комплект в `~/supabase`
     (docker compose), секреты сгенерированы `~/make-secrets.sh` и
     вписаны в `~/supabase/.env` (пароли записаны у Мурата, в чат и в
     репозиторий НЕ попадали). Панель Studio: `http://84.201.133.133:8000`,
     логин murat. Управление: `cd ~/supabase && docker compose ps`
     (ps/up -d/down/restart). ШАГ 2 ЗАКРЫТ (Веха 27).
   - ⚠️ Панель и API пока по http и открыты в интернет — до
     переключения приложения обязательно: домен + https (шаг 4) и
     решить, закрывать ли панель снаружи.
3. ⏳ СЛЕДУЮЩИЙ: перенос базы (схема, данные, RLS, триггеры — из
   `mingi-tau-full-2026-08-03.sql`, лежит на ВМ в `/home/murat`),
   Storage (аватары из `~/avatars-backup`), Edge Function
   `send-email-code` (+ секреты SMTP), проверка Realtime для liveService.
4. Переключение приложения на новый адрес, домен + https, сквозная
   проверка, автоснимки диска по расписанию.
5. Юридический чек-лист по ПДн (не забыть, лучше с юристом):
   уведомление Роскомнадзора, согласие на обработку при регистрации,
   дополнение политики конфиденциальности. Я (Клод) не юрист — по
   правовым вопросам нужна консультация специалиста по 152-ФЗ.

## Следующие шаги

1. **ПЕРЕЕЗД, шаг 3:** перенести в свой Supabase базу (полная копия
   уже на ВМ), аватары, Edge Function `send-email-code`; проверить
   Realtime.
2. Чат (`app/chat.tsx`): доработать функционально, перевести на
   дизайн-систему и на живое обновление через `liveService` (сейчас
   своя подписка). Проверка блокировки уже добавлена (Веха 17).
3. Метки на вкладках: число непрочитанных на «Чатах»
   (`chat_participants.last_read_at`).
4. Остатки модерации: `moderation-case-details` и прочие `moderation-*`
   (научить экран деталей и обращениям — тогда можно вернуть «Изучить»).
5. Мелкие служебные экраны: `request-name-change`, `access-restricted`.
6. Стена помощи — первая большая новая функция.
7. Веха деплоя: веб-версия на https → установка PWA.
